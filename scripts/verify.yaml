parameters:
  url: ''

steps:
- task: Boots@0
  displayName: install xamarin-android
  inputs:
    uri: ${{ parameters.url }}

- task: MSBuild@1
  displayName: verify
  inputs:
    solution: scripts/xa-version.targets
    msbuildArguments: '/v:minimal /nologo /t:Check /p:Expected=$(XA.Version)'
